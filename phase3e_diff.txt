diff --git a/package.json b/package.json
index 6a84f72..9ea362c 100644
--- a/package.json
+++ b/package.json
@@ -20,6 +20,7 @@
     "test:mediator:state": "node --import ./services/mediatorEngine/__tests__/path-alias-loader.mjs --experimental-strip-types --test services/mediatorEngine/__tests__/stateAnalyzer/analyzeState.test.ts",
     "test:mediator:reflection": "node --import ./services/mediatorEngine/__tests__/path-alias-loader.mjs --experimental-strip-types --test services/mediatorEngine/__tests__/reflection/runReflection.test.ts",
     "test:mediator:strategy": "node --import ./services/mediatorEngine/__tests__/path-alias-loader.mjs --experimental-strip-types --test services/mediatorEngine/__tests__/strategy/*.test.ts services/mediatorEngine/__tests__/goalContinuity/*.test.ts",
+    "test:mediator:goalContinuity": "node --import ./services/mediatorEngine/__tests__/path-alias-loader.mjs --experimental-strip-types --test services/mediatorEngine/__tests__/goalContinuity/*.test.ts",
     "test:mediator:safety": "node --import ./services/mediatorEngine/__tests__/path-alias-loader.mjs --experimental-strip-types --test services/mediatorEngine/__tests__/safety/evaluateSafety.test.ts",
     "test:mediator:prompt": "node --import ./services/mediatorEngine/__tests__/path-alias-loader.mjs --experimental-strip-types --test services/mediatorEngine/__tests__/promptComposer/*.test.ts",
     "test:mediator:llm": "node --import ./services/mediatorEngine/__tests__/path-alias-loader.mjs --experimental-strip-types --test services/mediatorEngine/__tests__/llm/*.test.ts",
diff --git a/services/mediatorEngine/__tests__/edge/denoImportCompat.test.ts b/services/mediatorEngine/__tests__/edge/denoImportCompat.test.ts
index f32dac4..d74c88e 100644
--- a/services/mediatorEngine/__tests__/edge/denoImportCompat.test.ts
+++ b/services/mediatorEngine/__tests__/edge/denoImportCompat.test.ts
@@ -97,6 +97,24 @@ describe('mediator-runtime — deploy bundle audit', () => {
     assert.equal(manifest.deployStrategy, 'esbuild-prebundle');
     assert.equal(manifest.extensionlessAtImportCountInDeployGraph, 0);
   });
+
+  it('generated bundle ma warunkowe FUTURE_PLAN/CLOSURE w buildGoalCandidateSet', () => {
+    assert.ok(existsSync(BUNDLE_PATH), 'bundle missing — run build:mediator:edge');
+    const bundle = readSource(BUNDLE_PATH);
+    const candidateSetStart = bundle.indexOf('function buildGoalCandidateSet');
+    assert.ok(candidateSetStart >= 0, 'buildGoalCandidateSet missing from bundle');
+
+    const candidateSetBlock = bundle.slice(candidateSetStart, candidateSetStart + 2500);
+    assert.ok(
+      !/appendCandidate\([^)]*\{ goal: "FUTURE_PLAN", kind: "fast_track" \}\);\s*appendCandidate\([^)]*\{ goal: "CLOSURE", kind: "fast_track" \}\)/.test(
+        candidateSetBlock
+      ),
+      'bundle must not unconditionally append FUTURE_PLAN and CLOSURE'
+    );
+    assert.match(candidateSetBlock, /acceptedByBoth/);
+    assert.match(candidateSetBlock, /completionDetected/);
+    assert.match(candidateSetBlock, /AGREEMENT/);
+  });
 });
 
 describe('mediator-runtime — static logging guard (source + bundle)', () => {
diff --git a/services/mediatorEngine/goalContinuity/adaptiveGoalSelection/buildGoalCandidateSet.ts b/services/mediatorEngine/goalContinuity/adaptiveGoalSelection/buildGoalCandidateSet.ts
index 17fd8d6..1dc6579 100644
--- a/services/mediatorEngine/goalContinuity/adaptiveGoalSelection/buildGoalCandidateSet.ts
+++ b/services/mediatorEngine/goalContinuity/adaptiveGoalSelection/buildGoalCandidateSet.ts
@@ -44,17 +44,20 @@ export function buildGoalCandidateSet(input: AdaptiveGoalSelectionInput): GoalCa
     appendCandidate(candidates, seen, { goal: baselineGoal, kind: 'baseline' });
   }
 
+  const hasAgreementCompleted = input.completedGoals.includes('AGREEMENT');
+  const closureAllowed = hasAgreementCompleted && input.completionDetected;
+
   const skipGoal = goalAtOffset(input.currentGoal, ADAPTIVE_GOAL_RULES.MAX_SKIP);
-  appendCandidate(candidates, seen, skipGoal ? { goal: skipGoal, kind: 'skip' } : null);
+  const skipAllowed = skipGoal && (skipGoal !== 'CLOSURE' || closureAllowed);
+  appendCandidate(candidates, seen, skipAllowed ? { goal: skipGoal, kind: 'skip' } : null);
 
   const regressGoal = goalAtOffset(input.currentGoal, -1);
   appendCandidate(candidates, seen, regressGoal ? { goal: regressGoal, kind: 'regress' } : null);
 
-  const hasAgreementCompleted = input.completedGoals.includes('AGREEMENT');
   if (input.acceptedByBoth || hasAgreementCompleted) {
     appendCandidate(candidates, seen, { goal: 'FUTURE_PLAN', kind: 'fast_track' });
   }
-  if (hasAgreementCompleted && input.completionDetected) {
+  if (closureAllowed) {
     appendCandidate(candidates, seen, { goal: 'CLOSURE', kind: 'fast_track' });
   }
 
diff --git a/supabase/functions/mediator-runtime/_generated/mediatorRuntime.bundle.ts b/supabase/functions/mediator-runtime/_generated/mediatorRuntime.bundle.ts
index 82ae03b..17c7c16 100644
--- a/supabase/functions/mediator-runtime/_generated/mediatorRuntime.bundle.ts
+++ b/supabase/functions/mediator-runtime/_generated/mediatorRuntime.bundle.ts
@@ -1532,12 +1532,19 @@ function buildGoalCandidateSet(input) {
   if (baselineGoal) {
     appendCandidate(candidates, seen, { goal: baselineGoal, kind: "baseline" });
   }
+  const hasAgreementCompleted = input.completedGoals.includes("AGREEMENT");
+  const closureAllowed = hasAgreementCompleted && input.completionDetected;
   const skipGoal = goalAtOffset(input.currentGoal, ADAPTIVE_GOAL_RULES.MAX_SKIP);
-  appendCandidate(candidates, seen, skipGoal ? { goal: skipGoal, kind: "skip" } : null);
+  const skipAllowed = skipGoal && (skipGoal !== "CLOSURE" || closureAllowed);
+  appendCandidate(candidates, seen, skipAllowed ? { goal: skipGoal, kind: "skip" } : null);
   const regressGoal = goalAtOffset(input.currentGoal, -1);
   appendCandidate(candidates, seen, regressGoal ? { goal: regressGoal, kind: "regress" } : null);
-  appendCandidate(candidates, seen, { goal: "FUTURE_PLAN", kind: "fast_track" });
-  appendCandidate(candidates, seen, { goal: "CLOSURE", kind: "fast_track" });
+  if (input.acceptedByBoth || hasAgreementCompleted) {
+    appendCandidate(candidates, seen, { goal: "FUTURE_PLAN", kind: "fast_track" });
+  }
+  if (closureAllowed) {
+    appendCandidate(candidates, seen, { goal: "CLOSURE", kind: "fast_track" });
+  }
   return candidates.filter((candidate) => isCandidateAllowed(candidate, guardInput));
 }
 
